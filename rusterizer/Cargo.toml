[package]
name = "rusterizer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["dylib"]

[dependencies]
glam = "*"
gltf = "1.1.0"
shared = { path = "../window/shared", features = ["import"] }
